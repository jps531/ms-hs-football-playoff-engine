# Auth Phase 2 — Deferred Features

Auth0 handles email verification, password reset, password change, and session
management. Nothing outside of Auth0's coverage remains deferred.

---

## HTTPS / SSL in nginx — done

HTTPS is live in production via Let's Encrypt/Certbot. See
[VM_DEPLOYMENT.md](VM_DEPLOYMENT.md#ssl-with-lets-encrypt) for the setup and
renewal procedure.
